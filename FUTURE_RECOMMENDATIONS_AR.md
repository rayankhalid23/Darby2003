# الوظائف البدائية/الناقصة والتوصيات المستقبلية لمشروع ربط السائقين المستقلين بأولياء الأمور

**تاريخ الإعداد:** 2026-09-19
**طبيعة المشروع:** منصة تربط سائقين مستقلين (Independent Drivers) بأولياء الأمور لنقل الأطفال، مع محفظة إلكترونية/تسوية مالية بين الطرفين ومنصة الوساطة، وطبقة قرار آلي (ذكاء اصطناعي) لتقييم أداء السائقين.

> **الغرض من هذا المستند:** توثيق رسمي لكل نقطة نعتبرها "وظيفة بدائية أو ناقصة" أو "توصية مستقبلية" — بحيث إذا أُثيرت أي من هذه النقاط في نقاش مع فريق العمل أو جهة تمويل/إدارة، يكون الجواب الجاهز: *"هذه النقطة موثّقة مسبقًا ضمن التوصيات المستقبلية للمشروع"*. تم بناء هذا المستند بقراءة فعلية للكود، ملفات الإعداد (`composer.json`, `package.json`, `.env.example`, `config/*.php`, `render.yaml`)، وقاموس البيانات الموجود في `docs/data-dictionary/`.

---

## القسم أ — الوظائف البدائية أو الناقصة (Gap Analysis)

### أ.1 — التواصل الفوري (Chat) بين الولي والسائق
**الحالة الحالية:** لا يوجد أي جدول `conversations`/`messages` في قاعدة البيانات، ولا أي طبقة بث لحظي (Pusher/Reverb/Soketi/WebSocket). ما هو منفّذ فعليًا هو فقط قائمة "غرف محادثة" مُركَّبة ديناميكيًا من جداول `requests` و`active_subscriptions` (`app/Http/Controllers/Api/Shared/ChatController.php` ← `SubscriptionRequestService::getParentChats/getDriverChats`) — بلا تخزين فعلي للرسائل، بلا سجل تاريخي، بلا إشعار لحظي لكل رسالة، بلا إرفاق صور/مستندات.
**ملاحظة إضافية موثّقة:** يوجد تعديل غير مُرحَّل حاليًا (`git diff`) يزيل فلترة حالة الاشتراك من `can_chat` ويجعلها `true` دائمًا — أي أن أي طرفين تبادلا طلب اشتراك واحد فقط (حتى لو مرفوض أو ملغى) سيظهران كغرفة محادثة متاحة. يجب تأكيد أن هذا مقصود قبل الدمج.
**التوصية:** تصميم جداول `conversations`/`messages` حقيقية مرتبطة بـ `requests`/`active_subscriptions`، ودمج طبقة بث لحظي (الأنسب هنا الاستفادة من Firestore الموجود أصلاً في المشروع للتتبع الحي بدل إدخال تقنية بث جديدة).

### أ.2 — بوابات الدفع
**الحالة الحالية:** طرق الدفع المعروضة (sadad, tadawul, moamalat) هي أسماء مُدخَلة (seeded) فقط — شحن محفظة الولي فوري بدون أي تكامل حقيقي مع بوابة دفع، بلا webhook تأكيد، بلا معرّف معاملة بنكي قابل للتحقق. إثبات دفع السائق (عند إعادة الشحن) صورة يدوية (`proof_image`) تُراجَع من أدمن يدويًا.
**التوصية:** قبل التوسع الحقيقي بأموال فعلية، دمج بوابة دفع محلية/إقليمية حقيقية (Sadad الفعلية أو مزوّد مماثل)، مع تخزين معرّف/إيصال المعاملة كدليل غير قابل للتلاعب.

### أ.3 — تخزين الملفات والوثائق الحساسة
**الحالة الحالية:** `FILESYSTEM_DISK=local` افتراضيًا، ومفاتيح AWS في `.env.example` فارغة — وثائق حساسة (بطاقات هوية، رخص قيادة، وثائق مركبات، صور إثبات دفع) تُخزَّن على قرص الخادم مباشرة، بلا CDN، بلا نسخ احتياطي سحابي منفصل، بلا تدوير مفاتيح وصول.
**التوصية:** الانتقال لتخزين سحابي (S3 أو مكافئ) مع صلاحيات وصول موقّعة مؤقتة (signed URLs) للوثائق الحساسة بدل الرابط المباشر.

### أ.4 — البنية التحتية للتشغيل
**الحالة الحالية (`render.yaml`):** خطة استضافة "free"، `SESSION_DRIVER=database`, `CACHE_STORE=database`, `QUEUE_CONNECTION=database` — لا Redis، لا تحجيم أفقي، فحص صحة بسيط (`/up`) فقط، بلا مراقبة أداء (APM) أو تتبع أخطاء (Sentry/Bugsnag) مُهيّأ.
**التوصية:** الانتقال لـ Redis كطبقة cache/queue قبل أي نمو حقيقي في عدد المستخدمين، ودمج أداة تتبع أخطاء وتنبيه فوري لفشل الـ jobs الحرجة (خصوصًا jobs المالية وjob تصنيف التقييمات بالذكاء الاصطناعي).

### أ.5 — التعدد اللغوي (i18n)
**الحالة الحالية:** `lang/ar/` يحتوي فقط على ملف `validation.php` — لا ترجمة كاملة لرسائل الـ API أو نصوص الإشعارات بالعربية، رغم أن السوق المستهدف (ليبيا) عربي بالكامل، و`APP_LOCALE=en` هي القيمة الافتراضية.
**التوصية:** استكمال ملفات الترجمة (`messages.php`, `notifications.php`...) وجعل `ar` هي اللغة الافتراضية للنشر في ليبيا.

### أ.6 — نظام التوجيه والخرائط (OSRM)
**الحالة الحالية:** خدمة OSRM ذاتية الاستضافة، محدودة ببيانات خريطة ليبيا فقط (`osrm-data/libya-260825.osm.pbf`) بتاريخ استخراج ثابت — لا آلية تحديث دوري لبيانات الخريطة، وأي توسّع جغرافي خارج ليبيا يتطلب بناء بيانات خريطة جديدة يدويًا بالكامل.
**التوصية:** جدولة تحديث دوري لملف OSM (OpenStreetMap يُحدَّث باستمرار)، والتخطيط المسبق لدعم مناطق جغرافية إضافية إن كانت التوسعة ضمن الخطة.

### أ.7 — إدارة مخطط قاعدة البيانات (Schema Drift)
**الحالة الحالية:** الجداول الجوهرية (`users`, `drivers`, `children`, `requests`, `active_subscriptions`, `trips`) ملفات إنشائها الأصلية موجودة في `database/migrations_v2`، وهو **مجلد غير مُحمَّل فعليًا** ضمن إعدادات Laravel النشطة (موثّق في `docs/data-dictionary/00-فهرس.md`). التوثيق الحالي لهذه الجداول مبني على استنتاج (مطابقة `$fillable` والـ accessors) وليس تأكيدًا مباشرًا من قاعدة البيانات الفعلية.
**التوصية:** توحيد كل الـ migrations ضمن مسار واحد فعّال، والتحقق من التطابق الفعلي عبر `php artisan migrate:status` قبل اعتماد أي توثيق رسمي للمخطط.

### أ.8 — طبقة القرار الآلي (الذكاء الاصطناعي)
**الحالة الحالية:** "القرار" النهائي (`AiDecisionService::determineFinalDecision`) منطق قواعد ثابتة (rule-based) بالكامل؛ نتيجة نموذج XGBoost (`/decision/predict`) تُستدعى وتُخزَّن في سجل التدقيق لكنها **لا تدخل فعليًا في اتخاذ القرار**. لا يوجد مسار استئناف رسمي للسائق ضد قرار تعليق/خصم تقييم. خدمة بايثون (`ai-service/`) بلا أي مصادقة.
**التوصية:** إما تفعيل تأثير نموذج XGBoost فعليًا في القرار، أو توضيح صراحة (في التوثيق والواجهة) أن القرار قائم على قواعد لا على تعلّم آلي بالكامل — تجنبًا لإشكال مصداقية قانونية لاحقًا؛ وإضافة مسار استئناف رسمي للسائق.

### أ.9 — الربط بين القرار الآلي والمالية
**الحالة الحالية:** سائق تحت تعليق احترازي (`ADMIN_REVIEW_REQUIRED`/`MODERATE_VIOLATION`) يبقى قادرًا على سحب أرباحه أو استلام أرباح رحلات معلّقة بشكل تلقائي — لا ربط بين حالة التعليق وحالة المحفظة/السحب. نافذة النزاع المالي 24 ساعة فقط، وبعدها لا يوجد مسار استرجاع (clawback) موثّق.
**التوصية:** مفصّلة في القسم ب أدناه (بند مالي).

### أ.10 — الموافقة الإدارية على العمليات المالية الحساسة
**الحالة الحالية:** موافقة السحب/الشحن تتم من طرف أدمن واحد فقط — لا وجود لمبدأ "الموافقة المزدوجة" (maker-checker) للعمليات المالية كبيرة القيمة.
**التوصية:** إضافة عتبة مبلغ (threshold) تتطلب موافقة أدمن ثانٍ للعمليات الكبيرة.

### أ.11 — التحقق من خلفية السائق (Background Check)
**الحالة الحالية:** التحقق من هوية/سجل السائق يعتمد بالكامل على رفع مستندات يدوي يراجعه أدمن — لا تكامل مع أي جهة تحقق رسمية (سجل عدلي، تحقق رخصة القيادة) بشكل آلي.
**التوصية:** خاصة بحساسية نقل الأطفال — دراسة إمكانية تكامل مستقبلي مع جهة تحقق رسمية ليبية إن توفرت، أو على الأقل إجراء تحقق دوري متكرر (لا مرة واحدة عند التسجيل فقط).

### أ.12 — الاختبارات و CI/CD
**الحالة الحالية:** يوجد 94 ملف اختبار (`tests/Feature`, `tests/Unit`) — تغطية معقولة، لكن لا يوجد أي pipeline تكامل مستمر (GitHub Actions أو ما شابه) في المستودع — الاختبارات تعمل يدويًا فقط، بلا فحص تلقائي قبل دمج أي تعديل.
**التوصية:** إضافة workflow بسيط يشغّل `composer test` تلقائيًا عند كل Pull Request.

### أ.13 — الأمان (تفصيل كامل في التقرير الأمني السابق `PROJECT_AUDIT_REPORT_AR.md`)
أبرزها: صلاحيات ناقصة على مسارات إدارة الذكاء الاصطناعي، غياب Rate Limiting على تسجيل الدخول/OTP، غياب 2FA لحسابات الأدمن، middleware قبول الشروط `terms.accepted` غير مفعّل فعليًا على أي مسار.

---

## القسم ب — التوصيات المستقبلية الرسمية (نسخة قابلة للاستشهاد بها)

> يمكن استخدام هذه القائمة حرفيًا كـ"سجل توصيات مستقبلية" رسمي للمشروع.

### أولوية أمنية (قبل أي نشر إنتاجي فعلي)
1. تصحيح صلاحيات مسارات `ai-reset` / `ai-alerts` / `ai-audits` في `routes/Admin.php`.
2. إضافة Rate Limiting على تسجيل الدخول، OTP، وتقديم التقييمات.
3. تفعيل middleware `terms.accepted` فعليًا على المسارات الحساسة (اشتراكات، دفعات).
4. إضافة مصادقة (shared secret أو شبكة داخلية) لخدمة الذكاء الاصطناعي بايثون.

### تطوير المنظومة المالية لضمان حقوق كل الأطراف (سائق مستقل ↔ ولي أمر ↔ المنصة)
5. ربط حالة التعليق الاحترازي (AI) بالمحفظة — منع السحب/التحرير التلقائي للأرباح المعلّقة أثناء مراجعة أمان مفتوحة.
6. ربط كل شكوى/تدقيق ذكاء اصطناعي بنزاع مالي (`TripDispute`) لنفس الرحلة تلقائيًا بدل بقاء المسارين منفصلين.
7. تمديد أو إضافة مسار نزاع مالي متأخر بعد نافذة الـ24 ساعة، مع آلية استرجاع (clawback) موثّقة لحالات الغش/الإهمال المؤكدة إداريًا.
8. إضافة موافقة إدارية مزدوجة (maker-checker) للعمليات المالية التي تتجاوز عتبة معينة.
9. عرض تفاصيل العمولة كاملة للسائق في كل دفعة (المبلغ، نسبة العمولة، الصافي) بدل بقائها ظاهرة للأدمن فقط.
10. دمج بوابة دفع حقيقية بإثبات معاملة بنكي قابل للتحقق قبل التوسع بأموال حقيقية على نطاق واسع.

### تطوير التواصل بين الطرفين
11. بناء منظومة محادثة حقيقية (جداول `conversations`/`messages` + بث لحظي عبر Firestore الموجود أصلاً)، مع تقييد الوصول بحالة الاشتراك الفعلية (وليس أي طلب سابق ولو مرفوضًا).

### تطوير طبقة الذكاء الاصطناعي وحوكمتها
12. توضيح/تفعيل الدور الفعلي لنموذج XGBoost في القرار النهائي.
13. إضافة مسار استئناف رسمي للسائق ضد قرارات التعليق/خصم التقييم الآلية.
14. توثيق كامل لمنظومة الذكاء الاصطناعي (تصنيف، قرار، تدقيق، تعافي ذاتي) — راجع القسم "ج" من `PROJECT_AUDIT_REPORT_AR.md`.

### تطوير البنية التحتية والجاهزية للنمو
15. الانتقال إلى Redis لطبقتي الـ cache/queue، وخطة استضافة تدعم التحجيم الأفقي، قبل أي نمو ملموس في عدد المستخدمين.
16. دمج أداة تتبع أخطاء وتنبيه فوري (Sentry أو مكافئ) خاصة لعمليات الطوابير المالية وقرار الذكاء الاصطناعي.
17. الانتقال لتخزين سحابي للوثائق الحساسة مع روابط وصول مؤقتة موقّعة.
18. استكمال ملفات الترجمة العربية الكاملة (رسائل API، إشعارات) وجعل العربية اللغة الافتراضية.
19. توحيد ملفات الـ migrations في مسار واحد فعّال والتحقق الدوري من تطابق التوثيق مع المخطط الفعلي لقاعدة البيانات.
20. إضافة pipeline تكامل مستمر (CI) يشغّل الاختبارات تلقائيًا على كل تعديل.
21. دراسة تكامل تحقق خلفية السائق مع جهة رسمية (نظرًا لحساسية نقل الأطفال).

---

## القسم ج — المراجع والتقنيات المستخدمة في المشروع بأكمله

### النواة (Backend)
| التقنية | الإصدار/الحزمة | الاستخدام |
|---|---|---|
| PHP | ^8.3 | لغة التطوير الأساسية |
| Laravel Framework | ^13.8 | الإطار الأساسي للـ backend |
| Laravel Sanctum | ^4.0 | مصادقة API عبر التوكنات |
| Spatie Laravel-Permission | ^7.4 | إدارة الأدوار والصلاحيات (RBAC) للأدمن/المشرفين |
| Bavix Laravel-Wallet | ^12.0 | البنية الأساسية للمحفظة الإلكترونية |
| Filament | ^5.7 | لوحة إدارة (Admin Panel) |
| Bezhansalleh Filament-Shield | ^4.3 | ربط الصلاحيات بلوحة Filament |
| LaravelDaily Laravel-Invoices | ^4.2 | توليد الفواتير المالية |
| Darkaonline L5-Swagger | ^11.0 | توثيق API تلقائي (Swagger/OpenAPI) |
| Knuckleswtf Scribe | ^5.10 (dev) | توليد توثيق API (`.scribe/`) |
| Ar-PHP | * | أدوات مساعدة للغة العربية |
| Twilio SDK | * | إرسال رسائل SMS (OTP) |
| Google Cloud Firestore | ^1.55/^2.0 | تخزين بيانات التتبع الحي للرحلات (GPS) والدردشة المستقبلية |
| Kreait Firebase-PHP | ^8.3 | التكامل مع Firebase (إشعارات FCM) |
| Laravel Breeze | ^2.4 (dev) | سقالة مصادقة أساسية |
| Pest/PHPUnit | ^12.5 (dev) | إطار الاختبارات |
| Laravel Pint | ^1.27 (dev) | تنسيق الكود (code style) |

### الواجهة الأمامية/أدوات البناء
| التقنية | الاستخدام |
|---|---|
| Vite ^5.0 + laravel-vite-plugin | بناء الأصول الأمامية (CSS/JS) |
| Axios | طلبات HTTP من الواجهة |
| Localtunnel | نفق محلي للتطوير (`npm run tunnel`) |

### خدمة الذكاء الاصطناعي (مستقلة — بايثون)
| التقنية | الاستخدام |
|---|---|
| FastAPI + Uvicorn | خادم API لخدمة الاستدلال (`ai-service/inference_api.py`, المنفذ 8001) |
| Transformers + PyTorch (torch) | تشغيل نموذج MARBERTv2 المُدرَّب (`driver_reviews_model.pt`) لتصنيف نصوص التقييمات (عربي) |
| XGBoost | نموذج قرار (`xgboost_decision_engine_87k.json`) لتقييم حالة السائق |
| python-multipart | معالجة الطلبات متعددة الأجزاء في FastAPI |

### التوجيه الجغرافي
| التقنية | الاستخدام |
|---|---|
| OSRM (Open Source Routing Machine) | محرك حساب المسارات/المسافات، مستضاف ذاتيًا (`app/Services/Shared/OsrmRoutingService.php`) |
| بيانات OpenStreetMap لليبيا | `osrm-data/libya-260825.osm.pbf` — قاعدة بيانات الخريطة المستخدمة لتوليد ملفات OSRM |

### خدمات خارجية / بنية تحتية (`config/services.php`, `.env.example`)
| الخدمة | الاستخدام |
|---|---|
| Twilio | إرسال SMS (أكواد OTP) |
| Firebase Cloud Messaging (HTTP v1) | الإشعارات الفورية (Push Notifications) |
| Google Cloud Firestore | التتبع الحي لموقع الرحلات |
| AWS S3 (مُعدّ لكن غير مفعّل فعليًا — مفاتيح فارغة في `.env.example`) | تخزين سحابي محتمل مستقبلًا |
| Postmark / Resend / AWS SES (مُعدّة كخيارات بريد، غير مفعّلة افتراضيًا) | خيارات مزوّد بريد إلكتروني |
| Slack (webhook) | تنبيهات (`services.slack.notifications`) |
| طرق دفع محلية (Sadad, Tadawul, Moamalat) | **أسماء مُدخَلة (seeded) فقط حاليًا — محاكاة، بلا تكامل بوابة دفع حقيقية** (انظر البند أ.2) |

### النشر والتشغيل
| العنصر | القيمة الحالية (`render.yaml`, `Dockerfile`) |
|---|---|
| منصة الاستضافة | Render.com (خطة "free" حاليًا) |
| قاعدة البيانات | MySQL (إنتاج) / SQLite (تطوير محلي افتراضيًا حسب `.env.example`) |
| Session/Cache/Queue driver | `database` (لا Redis) |
| نشر الحاوية | Docker (`Dockerfile` + `dockerfilePath`) |
| فحص الصحة | `/up` (مسار Laravel الافتراضي) |

---

*هذا المستند مبني على قراءة فعلية للكود وملفات الإعداد بتاريخ 2026-09-19، ويُكمّل تقرير المراجعة الأمنية `PROJECT_AUDIT_REPORT_AR.md`. يُنصح بمراجعته دوريًا (كل إصدار رئيسي) لإبقائه مطابقًا للواقع الفعلي للمشروع.*
