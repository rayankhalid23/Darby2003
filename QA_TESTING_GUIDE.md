# دليل اختبار النظام — بيانات QaComprehensiveSeeder

هذا الدليل يشرح كيفية استخدام البيانات التي زرعها `database/seeders/QaComprehensiveSeeder.php` لاختبار منطق التطبيق يدوياً بدقة. السيدر **إضافي بالكامل** (APPEND ONLY) — لم يحذف أو يُفرّغ أي جدول، ويعمل فوق `BaseSystemSeeder` (والبيانات الأخرى الموجودة مسبقاً مثل `DemoDataSeeder`/`ExtraDriversSeeder`) دون أي تعارض.

```bash
php artisan db:seed --class=QaComprehensiveSeeder
```

السيدر **قابل لإعادة التشغيل بأمان** (idempotent) — إذا شغّلتيه مرة أخرى، سيتخطى كل ما هو موجود بالفعل ولن يُكرر شيئاً. كلمة المرور لكل الحسابات: **`Password123!`**

> ملاحظة: التواريخ أدناه (الخميس، إلخ) محسوبة نسبةً لتاريخ التشغيل الفعلي (2026-09-19). إذا شغّلتِ السيدر في تاريخ مختلف ستتغير هذه التواريخ — راجعي دائماً تقرير آخر السيدر (يُطبع في الطرفية عند التشغيل) للحصول على القيم الفعلية المحدثة.

---

## 1) الحسابات

### أولياء الأمور الأساسيون (لسيناريو الاختبار اليدوي)

| البريد | الاسم | المنطقة | الأطفال |
|---|---|---|---|
| `parent3@darby.ly` | خالد إبراهيم محمد الككلي | بن عاشور | عمر (ولد، ابتدائي) / لينا (بنت، ابتدائي) / زياد (ولد، متوسط) / هدى (بنت، ابتدائي) |
| `parent4@darby.ly` | عبدالسلام أحمد محمود المرابط | حي الأندلس | يوسف (ولد، ابتدائي) / رهف (بنت، ابتدائي) / كريم (ولد، متوسط) / سلمى (بنت، ابتدائي) |

### أولياء أمور إضافيون (بيانات حجم/تنوع، ليسوا جزءاً من السيناريو الموجّه)
`parent5@darby.ly`، `parent6@darby.ly`، `parent7@darby.ly` — كل واحد له 1-2 طفل واشتراك نشط حقيقي (لتغذية عدد الرحلات والبيانات المالية).

### السائقون العشرون (`driver13@darby.ly` .. `driver32@darby.ly`)

| السائق | الغرض من الاختبار |
|---|---|
| driver13 | تطابق مثالي لمنطقة parent3 (بن عاشور) — **له اشتراك نشط حقيقي مع عمر ولينا** |
| driver14 | تطابق مثالي لمنطقة parent4 (حي الأندلس) — **له اشتراك نشط حقيقي مع يوسف ورهف** |
| driver15 | سائقة أنثى، `accepted_gender=female` — لاختبار فلتر الجنس |
| driver16 | بدون تكييف (`has_ac=0`) — **له اشتراك سابق مُلغى** مع سلمى (parent4 c4) |
| driver17 | منطقة بعيدة (أبو سليم) — **له طلب مرفوض** من parent3 (هدى) بسبب المنطقة |
| driver18 | `accepted_gender=male` فقط — يُستبعد عند اختيار أطفال مختلطي الجنس |
| driver19 | نوبة صباحية فقط (`afternoon_go=afternoon_return=0`) — **له اشتراك نشط** (نوبة واحدة/يوم فقط) |
| driver20 | مقاعده **ممتلئة بالكامل** للأسبوعين القادمين (مركبة سعة 6) — **له اشتراك نشط حقيقي** |
| driver21 | مقاعده ممتلئة **جزئياً** (بعض الأيام كاملة) — **له اشتراك نشط حقيقي** |
| driver22 | ⭐ **غائب معتمد يوم 2026-09-24 (الخميس)** — له اشتراك نشط مع زياد (parent3 c3) — محور سيناريو الاستثناء |
| driver23 | طلب غياب **معلّق** لتاريخ بعيد — **له اشتراك مستقبلي مجدول لم يبدأ بعد** |
| driver24 | رخصة **منتهية** (انتهت 2026-08-30) — يجب ألا يظهر في أي بحث إطلاقاً |
| driver25 | **موقوف حالياً** — قرار AI: `MODERATE_VIOLATION` (شكويان متطابقتا الفئة من وليّي أمر مختلفين) |
| driver26 | **إنذار رسمي** (AI: `FORMAL_WARNING`) لكنه غير موقوف — **له اشتراك نشط** مع كريم (parent4 c3) |
| driver27 | **مكافأة** (AI: `REWARD`) — **له اشتراك سابق مكتمل** حقيقي مع كريم (قبل انتقاله لـ driver26) |
| driver28 | مرشّح لالتئام تلقائي (AI self-healing، مخالفة عمرها 40 يوماً) — **له اشتراك نشط** |
| driver29 | تسجيل **قيد المراجعة** (`status=Pending`) — لن يظهر حتى تتم الموافقة |
| driver30 | تسجيل **مرفوض نهائياً** — لن يظهر إطلاقاً |
| driver31 | حافلة كبيرة (سعة 30) بتغطية جغرافية واسعة جداً — **سائق التغطية** لسيناريو الاستثناء + **له طلب معلّق** من parent4 (سلمى) |
| driver32 | وثائق (تأمين/فحص فني) **قاربت على الانتهاء** (~18 يوماً) — لاختبار تنبيهات انتهاء الصلاحية |

---

## 2) سيناريو الفلترة اليدوي (parent3 / parent4)

هذا هو الاختبار الذي طلبتيه بالتحديد: ادخلي كولي أمر، اختاري أطفالاً مختلفين وبيانات اشتراك مختلفة، وراقبي كيف تتغير نتيجة البحث عن سائق ومنطق الأهلية.

### أ) تسجيل الدخول كـ parent3، اختيار عمر (ولد) + لينا (بنت) — أطفال **مختلطو الجنس**
ابحثي عن سائق بتاريخ يقع ضمن 2026-09-05 → 2026-10-08 (فترة الاشتراكات النشطة الحالية):
- **يظهر**: driver13 (تطابق تام، `accepted_gender=both`)، driver31 (تغطية واسعة)، driver15 (أنثى، `accepted_gender=female` — تظهر لأنها تقبل "both" فقط إذا... **تنبيه**: driver15 تقبل إناثاً فقط، فستُستبعد تلقائياً لأن المجموعة فيها ولد).
- **يُستبعد**: driver18 (`accepted_gender=male` فقط — الفلتر الذكي يتطلب `both` عند تعدد الجنس ضمن الاختيار)، driver17 (منطقة أبو سليم بعيدة)، driver24 (رخصة منتهية)، driver29/driver30 (غير معتمدين)، driver25 (موقوف)، driver20 (سعته ممتلئة بالكامل خلال هذه الفترة).

### ب) غيّري الاختيار إلى عمر + زياد فقط (كلاهما **ذكور**)
- الآن driver18 (`accepted_gender=male`) **يظهر** أيضاً في النتائج (لأن كل الأطفال المختارين ذكور) — بينما في الحالة (أ) كان مستبعداً. هذا يُثبت أن منطق `applyChildrenSmartFilters` في `DriverMatchingService` يُعيد تقييم `accepted_gender` **حسب كل عملية اختيار جديدة**، وليس بشكل ثابت لكل سائق.
- ملاحظة: زياد له اشتراك نشط بالفعل مع driver22 — إذا حاولتِ إرسال طلب اشتراك جديد لزياد بتاريخ يقع ضمن 2026-09-05→2026-10-08 (باستثناء يوم الغياب) سيُرفض الطلب بسبب التعارض (راجعي القسم 3 أدناه).

### ج) بدّلي فلتر التكييف (`has_ac=true`)
- driver16 (بدون تكييف) يختفي من النتائج فوراً بغض النظر عن أي فلتر آخر.

### د) سجّلي الخروج، وسجّلي الدخول كـ parent4، اختاري يوسف + رهف
- **يظهر**: driver14 (تطابق تام لمنطقة حي الأندلس)، driver31 (تغطية واسعة تشمل حي الأندلس أيضاً).
- **يُستبعد**: driver13/driver22 (يخدمان منطقة بن عاشور فقط، لا تغطي حي الأندلس)، driver17 (أبو سليم)، إلخ.
- هذا يُثبت أن نفس السائق (driver31) يظهر لكلا العائلتين لأن تغطيته الجغرافية واسعة عمداً، بينما بقية السائقين مقيّدون بمناطقهم — نتيجة **مختلفة تماماً** عن نتيجة parent3 رغم استخدام نفس آلية البحث.

---

## 3) ⭐ سيناريو "تعارض الاشتراك النشط + استثناء غياب السائق"

هذا هو الاختبار الأهم الذي طلبتيه (منطق طلب الاشتراك يمنع تكرار اشتراك نشط، إلا في يوم يكون فيه السائق الأصلي مسجَّلاً غائباً).

**الإعداد الجاهز في قاعدة البيانات الآن:**
- الطفل **زياد خالد إبراهيم الككلي** (الابن الثالث لـ `parent3@darby.ly`) له اشتراك **نشط** (`active`) مع **driver22@darby.ly**، يغطي الفترة **2026-09-05 → 2026-10-08** (أيام الأحد-الخميس).
- **driver22** مسجَّل **غائباً معتمَداً** بالضبط يوم **2026-09-24 (الخميس)** — وهو يوم يقع داخل فترة اشتراك زياد النشط.
- **driver31@darby.ly** متاح تماماً في نفس المنطقة (بن عاشور + زاوية الدهماني) ولا يوجد لديه أي غياب في ذلك التاريخ.

### ✅ الاختبار الإيجابي (يجب أن يُقبل)
1. سجّلي الدخول كـ `parent3@darby.ly`.
2. أرسلي **طلب اشتراك جديد** (`subscription_type = single_day`) لزياد، مع **driver31**، بتاريخ البداية والنهاية = **2026-09-24**.
3. **النتيجة المتوقعة**: يُقبل الطلب بنجاح، **رغم** أن زياد لديه اشتراك نشط بالفعل مع driver22 — لأن `SubscriptionRequestService::validateChildActiveSubscriptionConflicts()` يستثني أي يوم يكون فيه **السائق الأصلي** (driver22) مسجَّلاً غائباً.

### ❌ الاختبار السلبي (يجب أن يُرفض)
1. كرّري نفس الخطوات تماماً، لكن بتاريخ **2026-09-27** (يوم أحد عادي — لا يوجد فيه أي غياب لـ driver22).
2. **النتيجة المتوقعة**: يُرفض الطلب برسالة من نوع:
   > "لا يمكن إرسال طلب الاشتراك: الطفل زياد ... لديه اشتراك نشط بالفعل مع السائق ... والسائق غير مسجل كغائب في هذه الأيام."

### 🔁 اختبار إضافي (فلترة السائق الغائب من البحث)
ابحثي عن سائق جديد (بحث عام وليس طلب اشتراك) لزياد بتاريخ يشمل **2026-09-24**: يجب ألا يظهر **driver22** إطلاقاً في نتائج البحث لتلك الفترة (لأن `DriverMatchingService::applySeatsAvailabilityFilter` يستبعد أي سائق له غياب `approved`/`pending` يتقاطع مع تاريخ البحث) — بينما يظهر بشكل طبيعي إذا بحثتِ بتاريخ لا يشمل يوم غيابه.

---

## 4) بيانات مالية جاهزة للفحص (كل جدول متسق مع الآخر)

- **محافظ (Wallets)**: كل الأولياء (parent3-7) بدأوا برصيد 3000 د.ل، وكل اشتراك مقبول (multi_day) خصم المبلغ فوراً وأودع صافي السائق فوراً في محفظته (يطابق `SubscriptionRequestService::holdSubscriptionFundsOnAcceptance` مسار multi_day الحقيقي بالضبط — لا يوجد escrow في هذا المسار).
- **دفتر الأستاذ (`financial_ledger`)**: سطرا `platform_commission` + `subscription_payment` لكل اشتراك مقبول.
- **خزينة المنصة (`master_escrow_vault`)**: `platform_revenue_pool` يتحرك مع كل عمولة.
- **مسار "اليوم الواحد" الكامل (escrow)**: حجز سابق لـ `parent6@darby.ly` (الطفل الأول) مع `driver31` — يُثبت مسار `parents_escrow_pool` → `platform_finance_trip_settlements` → `driver_available_pool` كاملاً (مختلف عن مسار multi_day أعلاه عمداً).
- **فواتير (`invoices`)**: `final`/`paid` لكل اشتراك مُسدَّد، `receipt`/`paid` لحجز اليوم الواحد.
- **طلبات شحن (`recharge_requests`)**: مكتمل (parent3) / معلّق (parent4) / فاشل (parent5).
- **طلبات سحب (`withdrawal_requests`)**: معتمد (driver13) / معلّق (driver14) / مرفوض (driver20، تجاوز الرصيد).
- **شحن سائقين (`driver_recharge_requests`)**: معتمد (driver21) / معلّق (driver19).

---

## 5) الرحلات (Trips) — كل الحالات

- **15 رحلة "in_progress" (نشطة الآن)** موزّعة على 8 اشتراكات نشطة مختلفة — تظهر فوراً في أي لوحة تتبع حي.
- **220 رحلة "completed"** (سابقة، موزّعة على أسبوعين ماضيين لكل اشتراك نشط + فترتان تاريخيتان مكتملتان بالكامل [driver13/عمر، driver27/كريم] + حجز اليوم الواحد).
- **265 رحلة "pending"** (قادمة/مجدولة، أسبوعان قادمان لكل اشتراك نشط + الاشتراك المستقبلي الكامل لـ parent7/driver23 الذي لم يبدأ إطلاقاً بعد).

---

## 6) اشتراكات مكتملة/ملغاة/معلّقة/مرفوضة

| الحالة | الأطراف | ملاحظة |
|---|---|---|
| مكتمل (تاريخ سابق) | parent3/عمر × driver13 | فترة سابقة انتهت قبل الاشتراك الحالي (نفس السائق، تجديد) |
| مكتمل (تاريخ سابق) | parent4/كريم × driver27 | ينتهي قبل انتقاله لـ driver26 — يفسّر تقييم driver27 الإيجابي |
| ملغى | parent4/سلمى × driver16 | أُلغي بعد أسبوع (بدون تكييف)، له فاتورة/رحلات الأسبوع الأول فقط |
| معلّق (طلب) | parent4/سلمى → driver31 | بانتظار قرار السائق |
| مرفوض (طلب) | parent3/هدى → driver17 | برسالة رفض تذكر خروج المنطقة عن نطاق التغطية |
| مستقبلي مجدول بالكامل | parent7/طفلها الوحيد × driver23 | يبدأ 2026-10-03، كل رحلاته `pending`، لم تبدأ أي رحلة بعد رغم أن المبلغ خُصم بالفعل عند "القبول" (يطابق سلوك التطبيق الحقيقي) |

---

## 7) تعليقات وشكاوى (`driver_reviews` / `support_tickets`)

8 تقييمات (تشمل التقييمات المرتبطة بقرارات AI) + 3 شكاوى دعم بحالات مختلفة (`in_progress`، `resolved`، `open`) بين أولياء أمور وسائقين في الاتجاهين.

---

**تذكير**: هذا السيدر لا يحذف شيئاً. لتنظيف بياناته لاحقاً (إن رغبتِ)، كل الكيانات الجديدة معرّفة بوضوح ببريد إلكتروني `driver13..driver32@darby.ly` و`parent3..parent7@darby.ly` فقط.
