# توثيق واجهات دفتر العناوين لولي الأمر (مع العنوان الرئيسي `is_default`)

> كل المسارات تحت `/api/parent` وتتطلب توكن Sanctum:
> `Authorization: Bearer <token>` و `Accept: application/json`.

---

## 1. قواعد العمل المعتمدة

| # | القاعدة |
|---|---------|
| 1 | لكل ولي أمر **عنوان رئيسي واحد فقط** (`is_default = true`). |
| 2 | **أول عنوان** يضيفه ولي الأمر يصبح رئيسياً تلقائياً، ولا يمكن رفض ذلك. |
| 3 | كل **أطفال ولي الأمر مسنَدون تلقائياً** إلى العنوان الرئيسي (`children.address_id`). |
| 4 | أي عنوان لاحق يُضاف بحالة `is_default = false` (عنوان ثانوي). |
| 5 | **تغيير العنوان الرئيسي مرفوض** إذا كان لدى أي طفل اشتراك مفعّل أو مجدول. |
| 6 | إذا لم توجد اشتراكات مفعّلة لكن توجد **طلبات اشتراك معلّقة**، تُلغى كلها تلقائياً (مع استرجاع المبالغ المحجوزة وإشعار السائقين) ثم يتم التغيير. |
| 7 | لا يمكن **إلغاء تفعيل** العنوان الرئيسي مباشرة — يُرقّى عنوان بديل بدلاً منه. |
| 8 | لا يمكن **حذف** العنوان الرئيسي طالما أطفال ولي الأمر مسنَدون إليه. حذفه بلا أطفال يُرقّي أقدم عنوان متبقٍّ تلقائياً. |

**تعريف «اشتراك مفعّل أو مجدول»:** صف في `active_subscriptions` بحالة `active` أو `paused`، **أو** طلب في `requests` بحالة `accepted` / `active` لم ينتهِ تاريخ `request_children.end_date` الخاص به.

**تعريف «طلب معلّق»:** طلب في `requests` بحالة `pending` أو `acquired`.

---

## 2. الأخطاء الموحّدة

كل أخطاء الأعمال ترجع بالشكل التالي:

```json
{
  "success": false,
  "status": false,
  "error_code": "ADDRESS_HAS_ACTIVE_SUBSCRIPTIONS",
  "message": "نص الرسالة بالعربية",
  "context": { "children": ["اسم الطفل"] }
}
```

| `error_code` | HTTP | الرسالة |
|---|---|---|
| `VALIDATION_ERROR` | 422 | أخطاء الحقول في مفتاح `errors` |
| `ADDRESS_HAS_ACTIVE_SUBSCRIPTIONS` | 422 | لا يمكن تغيير العنوان الرئيسي: يوجد اشتراكات مفعّلة للأطفال (…) على العنوان الحالي. لا يُسمح بتغيير العنوان إلا إذا لم يكن لدى أي من أطفالك اشتراك قائم. |
| `ADDRESS_DEFAULT_CANNOT_BE_UNSET` | 422 | لا يمكن إلغاء تفعيل العنوان الرئيسي مباشرة، يجب تعيين عنوان آخر كعنوان رئيسي بدلاً منه. |
| `ADDRESS_DEFAULT_CANNOT_BE_DELETED` | 422 | لا يمكن حذف العنوان الرئيسي لأن أطفالك مسنَدون إليه، يرجى تعيين عنوان آخر كعنوان رئيسي أولاً ثم إعادة المحاولة. |
| `ADDRESS_LINKED_TO_CHILD` | 422 | لا يمكن حذف هذا العنوان لأنه مرتبط بطفل مضاف في حسابك. |
| `ADDRESS_LABEL_DUPLICATED` | 422 | تعذر التعديل: لديك عنوان آخر مسجل مسبقاً باسم '…'. |
| `ADDRESS_LOCATION_DUPLICATED` | 422 | تعذر التعديل: هذا الموقع الجغرافي يتطابق مع موقع عنوان آخر مضاف لديك بالفعل. |
| `ADDRESS_DELETED` | 422 | لا يمكن تعيين عنوان محذوف كعنوان رئيسي. |
| `ADDRESS_NOT_OWNED` | 403 | هذا العنوان غير مسجل ضمن دفتر عناوينك. *(حماية دفاعية؛ الكنترولر يرجع 404 قبلها)* |
| `NOT_FOUND` | 404 | العنصر أو الرابط الذي تحاول الوصول إليه غير موجود أو تم حذفه. *(عنوان لا يملكه المستخدم)* |
| `UNAUTHENTICATED` | 401 | غير مصرح بالوصول، يرجى تسجيل الدخول أولاً. |

---

## 3. الاندبوينتات

### 3.1 عرض دفتر العناوين

`GET /api/parent/addresses`

**المدخلات:** لا شيء.

**المخرجات (200):** العنوان الرئيسي يأتي **أولاً** دائماً.

```json
{
  "success": true,
  "message": "تم جلب دفتر العناوين بنجاح.",
  "default_address_id": 12,
  "data": [
    { "id": 12, "label": "المنزل", "zone_id": 3, "zone_name": "شارع النصر",
      "lat": 32.885, "lng": 13.185, "is_default": true },
    { "id": 15, "label": "منزل الجدة", "zone_id": 3, "zone_name": "شارع النصر",
      "lat": 32.889, "lng": 13.189, "is_default": false }
  ]
}
```

> `default_address_id` يكون `null` إذا لم يكن لولي الأمر أي عنوان.

---

### 3.2 إضافة عنوان جديد

`POST /api/parent/addresses`

**المدخلات:**

| الحقل | إجباري | النوع | القيود |
|---|---|---|---|
| `label` | ✅ | string | 2–100 حرفاً، عربي + أرقام + `-` `/` فقط، غير مكرر لدى ولي الأمر |
| `lat` | ✅ | numeric | بين `-90` و `90`، وزوج (`lat`,`lng`) غير مكرر |
| `lng` | ✅ | numeric | بين `-180` و `180` |
| `zone_id` | ✅ | integer | موجود في `zones` |
| `is_default` | ❌ | boolean | `true` تجعله العنوان الرئيسي (تمر عبر حراسات القسم 3.4) |

**المخرجات (201):**

```json
{
  "success": true,
  "message": "تم إضافة العنوان الجديد وتعيينه كعنوان رئيسي وإسناد جميع أطفالك إليه.",
  "data": { "id": 21, "label": "المنزل", "zone_id": 3, "zone_name": "شارع النصر",
            "lat": 32.885, "lng": 13.185, "is_default": true }
}
```

للعناوين الثانوية تكون الرسالة: `"تم إضافة العنوان الجديد بنجاح كعنوان ثانوي."`

**سلوك مهم:**
- إذا لم يكن لدى ولي الأمر أي عنوان ⇒ `is_default` تُفرض `true` مهما كانت القيمة المرسلة، ويُسنَد كل الأطفال إليه.
- إذا أُرسلت `is_default = true` ووُجدت اشتراكات مفعّلة ⇒ **يُرفض الطلب بالكامل ولا يُنشأ العنوان** (العملية ذرّية داخل Transaction).

**رسائل التحقق:**
`مسمى العنوان مطلوب.` · `مسمى العنوان يجب ألا يقل عن حرفين.` · `مسمى العنوان يجب ألا يتجاوز 100 حرف.` · `مسمى العنوان يجب أن يكون بالعربية (ويمكن أن يتضمن أرقاماً).` · `اسم العنوان مسجل لديك مسبقاً.` · `إحداثيات خط العرض مطلوبة.` · `إحداثيات خط العرض غير صالحة جغرافياً.` · `هذا الموقع الجغرافي مسجل لديك مسبقاً.` · `إحداثيات خط الطول مطلوبة.` · `إحداثيات خط الطول غير صالحة جغرافياً.` · `يرجى تحديد المنطقة الجغرافية التابع لها العنوان.` · `المنطقة الجغرافية المختارة غير مسجلة بالنظام.` · `قيمة تفعيل العنوان الرئيسي يجب أن تكون true أو false.`

---

### 3.3 تعديل عنوان (تعديل جزئي)

`POST | PUT | PATCH /api/parent/addresses/{address}`

**المدخلات — كل الحقول اختيارية**، ويُعدَّل ما يُرسَل فقط:

| الحقل | إجباري | النوع | القيود |
|---|---|---|---|
| `label` | ❌ | string | نفس قيود الإضافة (يُستثنى العنوان نفسه من التكرار) |
| `lat` | ❌ | numeric | بين `-90` و `90` |
| `lng` | ❌ | numeric | بين `-180` و `180` |
| `zone_id` | ❌ | integer\|null | موجود في `zones` |
| `is_default` | ❌ | boolean | `true` ⇒ تبديل العنوان الرئيسي بكل الحراسات · `false` ⇒ **مرفوض** على العنوان الرئيسي الحالي |

**المخرجات (200):**

```json
{
  "success": true,
  "message": "تم تحديث بيانات العنوان بنجاح.",
  "data": { "id": 15, "label": "منزل الجدة", "zone_id": 3, "zone_name": "شارع النصر",
            "lat": 32.889, "lng": 13.189, "is_default": false }
}
```

---

### 3.4 تعيين العنوان الرئيسي ⭐ (الاندبوينت الجديد)

`POST | PUT | PATCH /api/parent/addresses/{address}/set-default`

**المدخلات:** لا شيء (المعرّف في المسار فقط).

**تسلسل التنفيذ:**
1. رفض إن كان لدى أي طفل اشتراك مفعّل/مجدول ⇒ `422 ADDRESS_HAS_ACTIVE_SUBSCRIPTIONS`.
2. وإلا: إلغاء **كل** طلبات الاشتراك المعلّقة (`pending` / `acquired`) مع استرجاع المبالغ المحجوزة وإشعار السائقين.
3. نقل `is_default` إلى العنوان الجديد وإسناد **كل الأطفال** إليه.

**المخرجات (200) — حالة وجود طلبات ملغاة:**

```json
{
  "success": true,
  "message": "تم إلغاء جميع طلبات الاشتراك المرتبطة بالعنوان السابق (عددها 2)، وتم تعيين العنوان الرئيسي الجديد وإسناد جميع أطفالك إليه.",
  "changed": true,
  "cancelled_requests_count": 2,
  "cancelled_request_ids": [41, 42],
  "reassigned_children_count": 3,
  "data": { "id": 15, "label": "منزل الجدة", "zone_id": 3, "zone_name": "شارع النصر",
            "lat": 32.889, "lng": 13.189, "is_default": true }
}
```

**بلا طلبات معلّقة:** `"تم تعيين العنوان الرئيسي بنجاح، وتم إسناد جميع أطفالك إليه."` مع `cancelled_requests_count = 0`.

**العنوان رئيسي أصلاً:** `changed = false` ورسالة `"هذا العنوان هو العنوان الرئيسي المفعل لديك بالفعل."` — ولا يُلغى أي طلب.

**حالة الرفض (422):**

```json
{
  "success": false, "status": false,
  "error_code": "ADDRESS_HAS_ACTIVE_SUBSCRIPTIONS",
  "message": "لا يمكن تغيير العنوان الرئيسي: يوجد اشتراكات مفعّلة للأطفال (محمد علي سالم) على العنوان الحالي. لا يُسمح بتغيير العنوان إلا إذا لم يكن لدى أي من أطفالك اشتراك قائم.",
  "context": { "children": ["محمد علي سالم"] }
}
```

---

### 3.5 حذف عنوان (حذف ناعم)

`DELETE /api/parent/addresses/{address}`

**المدخلات:** لا شيء.

**المخرجات (200):** `{ "success": true, "message": "تم حذف العنوان بنجاح." }`

**السلوك:**
- عنوان مرتبط بطفل ⇒ رفض (`ADDRESS_DEFAULT_CANNOT_BE_DELETED` إن كان رئيسياً، وإلا `ADDRESS_LINKED_TO_CHILD`).
- حذف عنوان رئيسي **بلا أطفال** ⇒ يُحذف، ويُرقّى أقدم عنوان متبقٍّ إلى رئيسي تلقائياً.
- الحذف ناعم، ويمكن إعادة إضافة نفس المسمى والإحداثيات لاحقاً.

---

## 4. الاندبوينتات المتأثرة (الأطفال)

### 4.1 `POST /api/parent/children`

`address_id` **لم يعد إجبارياً**:

| الحقل | قبل | بعد |
|---|---|---|
| `address_id` | إجباري | **اختياري** — يُملأ تلقائياً من العنوان الرئيسي؛ وإن أُرسل وجب أن يطابقه |

**رسائل جديدة (422):**
- `لا يوجد عنوان رئيسي مفعّل في حسابك، يرجى إضافة عنوان أولاً قبل إضافة طفل.`
- `لا يمكن إسناد الطفل إلا للعنوان الرئيسي المفعّل [المنزل]، يرجى تعيين العنوان المطلوب كعنوان رئيسي أولاً.`

باقي حقول الاندبوينت لم تتغير.

### 4.2 `POST | PUT | PATCH /api/parent/children/{id}`

`address_id` اختياري كما كان، لكنه الآن **مقيَّد بالعنوان الرئيسي** وتظهر نفس الرسالتين أعلاه عند المخالفة.

> نقل الأطفال بين العناوين لم يعد يتم من اندبوينت الطفل، بل حصراً من `set-default` (القسم 3.4) لأنه المكان الذي تُطبَّق فيه حراسات الاشتراكات.

---

## 5. تغييرات قاعدة البيانات والاعتماديات

| الملف | التغيير |
|---|---|
| `database/migrations/2026_09_06_100000_add_is_default_to_addresses_table.php` | إعادة عمود `is_default` + فهرس `(user_id, is_default)` + ردم بيانات يضمن عنواناً رئيسياً واحداً لكل مستخدم ويسند الأطفال إليه |
| `app/Services/Parent/AddressService.php` | منطق العنوان الرئيسي كاملاً (`setDefaultAddress`, `ensureDefaultAddress`, `resolveEffectiveDefault`, `assignChildrenToAddress`) |
| `app/Exceptions/Parent/AddressOperationException.php` | استثناء أعمال جديد — كانت رسائل مثل «لا يمكن حذف عنوان مرتبط بطفل» تعود سابقاً كـ **500** برسالة مبهمة |
| `app/Services/Shared/LocationChangeService.php` | إصلاح استعلام كان يستخدم `addresses.parent_id` (عمود غير موجود) بدل `user_id` |
| `app/Models/User.php` · `app/Models/Parent/ParentModel.php` | إصلاح علاقة `addresses()` لنفس السبب + إضافة علاقة `defaultAddress()` |

> **ملاحظة:** عمود `is_default` كان قد أُسقِط في مهاجرة `2026_08_21_000002` بينما بقي مستخدماً في موديل `Address` وفي 8 seeders و4 ملفات اختبار — فكانت كل كتابة تفشل بـ «Unknown column». هذه المهاجرة تصلح ذلك أيضاً.

عناوين السائقين تعيش في جدول منفصل (`driver_addresses`) ولم تتأثر بهذا التغيير.

---

## 6. التغطية الاختبارية

`tests/Feature/ParentDefaultAddressTest.php` — **17 اختباراً، جميعها ناجحة**:

أول عنوان يصبح رئيسياً · العنوان الثاني ثانوي · الإضافة بـ `is_default=true` تبدّل وتعيد الإسناد · `set-default` ينقل كل الأطفال · الرفض عند اشتراك مفعّل · الرفض عند طلب مقبول مجدول · إلغاء الطلبات المعلّقة عند التبديل · تعيين العنوان الرئيسي الحالي لا يفعل شيئاً · منع حذف الرئيسي مع أطفال · ترقية العنوان التالي عند حذف رئيسي بلا أطفال · حذف الثانوي مسموح · منع إلغاء التفعيل المباشر · التعديل يحترم حارس الاشتراكات · ترتيب القائمة و`default_address_id` · حماية IDOR · الإسناد التلقائي للطفل · رفض إسناد الطفل لعنوان ثانوي.
