# تقرير مراجعة شاملة للمشروع — الوظائف، الثغرات، والتوصيات المستقبلية

**تاريخ التقرير:** 2026-09-19
**النطاق:** مراجعة كاملة للكود الفعلي (Laravel backend + خدمة الذكاء الاصطناعي بايثون)، مش مجرد التوثيق الموجود.

---

## 0. ملخص تنفيذي — أهم شيء لازم يتعالج فورًا

قبل كل شيء، لقينا **ثغرة أمنية حرجة (Critical)** لازم تتصلح قبل أي نشر أو استعمال حقيقي للمنظومة الجديدة:

> **أي مستخدم مسجّل (ولي أمر أو حتى سائق) يقدر يستعمل التوكن متاعه العادي باش:**
> - يفك تعليق أي سائق (بما فيه نفسه) عبر `POST /admin/drivers/{id}/ai-reset`
> - يقرا كل سجلات `AiDecisionAudit` (بيانات شخصية ونصوص تقييمات لسائقين/أولياء آخرين)
> - يحل تنبيهات الذكاء الاصطناعي (AI alerts) الخاصة بأي سائق

السبب: مسارات `ai-reset` و`ai-alerts/*` و`ai-audits/*` في `routes/Admin.php` (الأسطر 132-155) ناقصة `middleware('permission:...')` اللي موجود في كل باقي مسارات الأدمن. راهي فقط جوا `auth:sanctum`، يعني أي توكن صالح (ولي، سائق، أو أدمن) يكفي. **هذي لازم تتصلح قبل أي حاجة أخرى.**

---

## 1. الخريطة الوظيفية للمشروع

المشروع منصة نقل مدرسي (Laravel) تربط بين **الأولياء** و**السائقين**، مع طبقة قرار بالذكاء الاصطناعي جديدة لتصنيف التقييمات واتخاذ قرارات تلقائية بخصوص السائقين.

| المجال | الوظيفة الحالية |
|---|---|
| **المصادقة (Auth)** | تسجيل دخول منفصل لكل دور (ولي/سائق/أدمن)، OTP للتسجيل واسترجاع كلمة السر، Sanctum tokens، تغيير البريد عبر روابط موقّعة (signed URLs) |
| **ملف السائق والمطابقة** | تسجيل ومراجعة وثائق السائق، موافقة الأدمن، `DriverMatchingService` يفلتر السائقين حسب الحالة والثقة وصلاحية الرخصة **ويستثني تلقائيًا أي سائق موقوف من طرف الذكاء الاصطناعي** |
| **الرحلات (Trips)** | توليد يومي للرحلات، بداية/استلام/تسليم مع QR، تتبع GPS حي (Firestore + DB)، تبليغ الغياب، تبليغ الأعطال مع نشر سائق بديل طارئ |
| **الشروط والأحكام** | نظام إصدارات (draft → publish → أرشفة تلقائية)، تسجيل IP/user-agent كدليل قانوني — **لكن middleware التفعيل `terms.accepted` غير مربوط بأي مسار فعليًا (كود ميت)** |
| **التقييمات وقرار الذكاء الاصطناعي** | انظر القسم 2 بالتفصيل |
| **الإشعارات** | `NotificationService` مركزي، إشعار DB فوري + push عبر job منفصل (idempotent) |
| **المالية** | نظام ledger مزدوج القيد + escrow pools (`master_escrow_vault`)، معاملات DB محمية بـ `lockForUpdate`، استرجاع، تسوية شهرية، فحص ملاءة يومي — **منظومة قوية نسبيًا لكن غير مربوطة بطبقة القرار الجديدة (انظر القسم 4)** |
| **إدارة الأدمن والمشرفين** | RBAC عبر middleware لكل مجموعة مسارات، سجل تدقيق admin-audit-logs |
| **الشكاوى** | نظام شكاوى منفصل تمامًا عن نظام قرار الذكاء الاصطناعي (تحذير/تعليق/رفض يدوي من الأدمن) |
| **الجغرافيا** | بحث موحّد بلديات/مناطق، CRUD كامل، عناوين الأولياء/السائقين |

### آلية قرار الذكاء الاصطناعي (الجزء الجديد، الأكثر تعقيدًا وغير موثّق إطلاقًا)

1. الولي يكتب تقييم → `ClassifyDriverReviewJob` يُرسل النص لخدمة بايثون منفصلة (`ai-service/`, FastAPI, نموذج MARBERTv2) للتصنيف (إيجابي/سلبي/محايد، شدة 0-2، فئة كالسلامة/الانضباط/السلوك...).
2. `AiDecisionService` يحسب نافذة متحركة من 15 يوم (نسب إيجابي/سلبي، عدد أولياء مختلفين لكل فئة)، يستدعي نموذج XGBoost عبر نفس الخدمة، ثم **منطق قواعد صريح (rule-based)** يحدد القرار النهائي: `NO_ACTION`, `REWARD`, `MODERATE_VIOLATION`, `FORMAL_WARNING`, أو `ADMIN_REVIEW_REQUIRED` (تعليق احترازي 24 ساعة).
3. كل قرار يسجَّل في جدول `ai_decision_audits` (تدقيق كامل: المتجهات، الاحتمالات، القرار، قبل/بعد التقييم).
4. تعافي ذاتي بعد 30 يوم بدون تكرار المخالفة، واسترجاع تلقائي كل 15 دقيقة (`drivers:restore-suspended`) للتعليق المؤقت المنتهي.
5. الأدمن يقدر يعيد الضبط يدويًا عبر `ai-reset`.

---

## 2. الثغرات والفجوات غير المعالجة (مرتّبة حسب الخطورة)

### 🔴 حرجة (Critical)

**2.1 — تحكم وصول مكسور على كامل مسارات سياسة الذكاء الاصطناعي**
`routes/Admin.php:132-155` — مسارات `ai-reset`, `ai-alerts/*`, `ai-audits/*` بلا `permission` middleware. أي مستخدم مصادق (مش بالضرورة أدمن) يقدر يستدعيها.
**الحل:** إضافة `->middleware('permission:drivers.review_initial')` أو صلاحية مخصّصة `ai.manage` لكل المجموعات الثلاث، بنفس نمط باقي الملف.

**2.2 — تعليق تلقائي بتقييم واحد فقط، بلا حد أقصى للتقييمات وبلا مراجعة بشرية مسبقة**
`StoreDriverReviewRequest.php:27-38` يلغي قيد "تقييم واحد لكل سائق" لأي ولي كان مشترك ولو مرة — يعني تقييمات غير محدودة بلا فترة انتظار. `AiDecisionService::determineFinalDecision` يفعّل `ADMIN_REVIEW_REQUIRED` (تعليق فوري 24 ساعة + خصم 10% من التقييم) من **تقييم سلبي واحد فقط** بفئة "سلامة" أو شدة قصوى — بخلاف `MODERATE_VIOLATION` اللي يتطلب أولياء متعددين. دمج هذا مع الثغرة 2.1 يفتح الباب لولي واحد ناقم باش يعلّق سائق بتقييمات موجّهة (adversarial) بشكل متكرر.
**الحل:** تطلّب تأكيد من أكثر من ولي حتى لفئة السلامة، تحديد معدّل التقييمات لكل ولي/سائق، وجعل التعليق الاحترازي يتطلب تأكيد أدمن ضمن مهلة قصيرة بدل التطبيق الفوري.

**2.3 — خدمة الذكاء الاصطناعي (بايثون) بلا أي مصادقة**
`ai-service/inference_api.py` تعمل على `0.0.0.0:8001` بلا مفتاح API ولا CORS ولا تحديد معدّل. إذا كان المنفذ متاح على الشبكة، أي طرف يقدر يجرّب نصوص "adversarial" قبل إرسالها عبر التطبيق، أو يغرقها بطلبات (DoS رخيص).
**الحل:** حماية الخدمة بـ shared secret / شبكة داخلية فقط، وتحديد طول المدخلات ومعدل الطلبات.

### 🟠 عالية (High)

**2.4 — بلا حد لمعدل الطلبات (rate limiting) على تسجيل الدخول/OTP** — `routes/api.php:33-41`، لا يوجد `throttle` على `/auth/login` أو مسارات OTP إطلاقًا في المشروع كله. يسمح بمحاولات غير محدودة لتخمين كلمات السر/الأكواد.

**2.5 — "قرار الذكاء الاصطناعي" في الواقع قرار قواعد ثابتة، لا علاقة لنتيجة XGBoost به** — `AiDecisionService.php:108` يمرر `$modelCode` لكن لا يُستخدم إطلاقًا داخل `determineFinalDecision()`. سجل التدقيق (`AiDecisionAudit`) يعرض احتمالات النموذج وكأنها أثّرت في القرار، بينما القرار الفعلي منطق قواعد صرف — مشكلة في المصداقية القانونية للسجل إذا استُعمل كدليل في نزاع مع سائق.

**2.6 — تسريب تفاصيل الأخطاء + `.env.example` يفعّل debug افتراضيًا** — `LoginController.php:126-135` يرجّع `trace/file/line` كاملة لما `APP_DEBUG=true`، و`.env.example` يحدد `APP_ENV=local`/`APP_DEBUG=true` كقيمة افتراضية بلا تحذير — نشر مبني على هذا القالب مباشرة يكشف تفاصيل داخلية.

### 🟡 متوسطة (Medium)

**2.7 — middleware قبول الشروط موجود لكنه كود ميت** — `EnsureTermsAccepted` مُعرّف بالكامل ومسجّل كـ alias `terms.accepted`، لكن بحث شامل في كل ملفات المسارات ما لقى أي استعمال له. الحماية القانونية المقصودة (منع إجراءات حساسة قبل الموافقة على الشروط) غير مفعّلة فعليًا.

**2.8 — إمكانية حذف الدليل وراء قرار تعليق آلي بشكل نهائي** — `DriverReviewController::destroy()` يستعمل `forceDelete()` رغم أن الموديل يدعم `SoftDeletes` — الولي اللي كتب التقييم يقدر يمحيه نهائيًا حتى لو أدّى لتعليق السائق (يبقى نص جزئي فقط داخل `AiDecisionAudit.action_details`).

### 🟢 منخفضة (Low)

**2.9** — `CheckAdminPermission` "fail-open" إذا middleware مُستدعى بلا سلسلة صلاحية — يفسّر جزئيًا كيف وقعت 2.1.
**2.10** — `FinancialController::processRecharge()` يقرأ المدخلات مباشرة بـ `request('action', 'complete')` بدل `FormRequest` مخصص (بخلاف `processWithdrawal`) — تأثير محدود لأن المسار محمي بصلاحية، لكن غير متّسق.

---

## 3. فجوات المنظومة المالية تحديدًا (تخص حقوق الأطراف)

المنظومة المالية **أقوى بكثير من المتوقع** لمشروع بهذا الحجم: ledger مزدوج القيد غير قابل للتعديل، معاملات DB محمية بـ `lockForUpdate`، عمولة تُقرأ من مصدر واحد ومُسجَّلة وقت التنفيذ (قابلة للتدقيق لاحقًا)، فحص ملاءة يومي. لكن الفجوة الأساسية:

**3.1 — طبقة قرار الذكاء الاصطناعي غير مربوطة إطلاقًا بالجانب المالي.**
- سائق موقوف احتياطيًا (`ADMIN_REVIEW_REQUIRED` أو `MODERATE_VIOLATION`) يقدر يقدّم طلب سحب فلوس ويتوافق عليه الأدمن بلا أي تنبيه بأنه تحت مراجعة أمان حالية.
- `releasePendingTripEscrows()` (cron كل ساعة) يحرّر أرباح الرحلات المعلّقة لسائق موقوف بلا أي فحص لحالة التعليق — الفلوس تتحرك حسب مؤقّت ثابت بغض النظر عن تحقيق أمان مفتوح.
- إذا عُلّق سائق نهائيًا لاحقًا بسبب مثبت، لا يوجد مسار لتجميد أو استرجاع (clawback) أموال سُحبت مسبقًا مرتبطة بالحادثة.

**3.2 — لا ربط بين شكوى/تدقيق الذكاء الاصطناعي ونزاع مالي لنفس الرحلة** — `AiDecisionAudit` و`ComplaintController` منفصلان تمامًا عن `TripDispute`/`TripEscrowHold` — سائق ممكن يتلقى إنذار سلامة بينما دفع الرحلة يكتمل عاديًا بلا أي وقفة.

**3.3 — نافذة النزاع محدودة بـ24 ساعة فقط** (`ESCROW_HOLD_HOURS`) — بعدها الفلوس محرّرة نهائيًا، فشكوى متأخرة (مكتشفة بعد أيام) بلا أي حل مالي، فقط منع مستقبلي.

**3.4 — بلا مسار عكسي (reversal) للسحب بعد الموافقة** — إذا تبيّن لاحقًا أن السائق ارتكب المخالفة المُراجَعة، لا توجد آلية لاسترجاع فلوس خرجت من المنصة.

**3.5 — شفافية العمولة للسائق ضعيفة** — تفاصيل العمولة محفوظة في ledger metadata لكن ظاهرة للأدمن فقط (بالسنتيمات)، لا تظهر للسائق في الفاتورة/الأرباح.

**3.6 — "إثبات الدفع" غير متماثل** — السائق يرفق `proof_image` إجباري عند إعادة الشحن، بينما شحن الولي فوري (محاكاة بوابة دفع، مش بوابة حقيقية بعد) — عند ربط بوابة دفع حقيقية، لازم إثبات قابل للتحقق (معرّف معاملة بنكي) لا مجرد رقم فاتورة داخلي.

---

## 4. المراجع/الفجوات اللي لازم تنحط في التوثيق (docs/)

الفحص أكّد إن **كل التوثيق الموجود** (FRONTEND_*.md, FINANCIAL_API*.md, TRIPS_LOGIC_REPORT_AR.md...) محدّث نسبيًا ويغطي: المالية، RBAC، الجغرافيا، الإشعارات، دورة حياة الرحلات، الشكاوى، أدلة السائق/الولي.

**لكن ما يخص منظومة الذكاء الاصطناعي بالكامل — صفر توثيق:**

1. خط أنابيب تصنيف التقييمات (`ReviewClassifierService`, نموذج MARBERTv2) — كيفاش يشتغل، الحقول اللي يرجّعها.
2. طبقة القرار (`AiDecisionService`) — الأكواد الخمسة للقرار، منطق النافذة 15 يوم، منطق الحوكمة (governance overrides).
3. جدول ومسارات `ai_decision_audits` / `GET /admin/ai-audits` — الغرض منها، مين يقدر يوصلها (يلزم يتصلح أولاً حسب 2.1).
4. مسارات `AdminAlert` الخاصة بالذكاء الاصطناعي (`/admin/ai-alerts/*`) وأنواع التنبيهات.
5. آلية التعليق/الإنذار/التعافي الذاتي (`suspended_until`, `active_warnings_count`, `suspension_count`, تعافي 30 يوم).
6. `POST /admin/drivers/{id}/ai-reset` (إعادة تأهيل يدوية).
7. أمر `drivers:restore-suspended` المجدول والفرق بين التعليق المؤقت والدائم.
8. خدمة `ai-service/` بايثون المستقلة (المسارات، ملفات النماذج، النشر/التشغيل) — لا يوجد README فيها حاليًا.
9. أوامر الاختبار للمطورين (`ai:test`, `ai:test-pipeline`, `ai:test-decision-layer`).
10. العلاقة بين حالة تعليق الذكاء الاصطناعي وظهور السائق في نتائج البحث (`DriverMatchingService`) — غير موثّقة، تُكتشف فقط بقراءة الكود.

**توصية:** إنشاء `docs/AI_DECISION_SYSTEM_API.md` (بنفس نمط `FINANCIAL_API.md`) يغطي النقاط أعلاه، بالإضافة لملاحظة أمنية صريحة عن ضرورة الصلاحيات على هاي المسارات.

---

## 5. توصيات مستقبلية للمشروع

### أمنية (أولوية فورية)
- تصحيح ثغرة 2.1 قبل أي نشر أو استعمال حقيقي — هذا وحدو يكفي يعرّض بيانات كل السائقين/الأولياء.
- إضافة rate limiting شامل (تسجيل دخول، OTP، تقديم التقييمات).
- تفعيل `terms.accepted` middleware فعليًا على المسارات الحساسة (اشتراكات، دفعات) لضمان الحماية القانونية المقصودة أصلاً.

### تطوير المنظومة المالية لضمان حقوق كل الأطراف (حسب طلبك)
1. **ربط حالة التعليق الاحترازي بالمالية**: منع السحب/تحرير الأرباح المعلّقة تلقائيًا لسائق تحت `ADMIN_REVIEW_REQUIRED` أو `MODERATE_VIOLATION` غير محلول.
2. **ربط الشكاوى/تدقيق الذكاء الاصطناعي بالنزاعات المالية** عبر `trip_id` — أي علامة سلامة تفتح تلقائيًا (أو تُعلّم) نزاع/تجميد مالي لنفس الرحلة.
3. **تمديد أو إضافة مسار نزاع متأخر** بعد نافذة الـ24 ساعة، مع آلية استرجاع (clawback) موثّقة للحالات المؤكدة من الأدمن — بدل تعديل قاعدة البيانات يدويًا.
4. **إضافة نوع عملية ledger صريح للعكس (`withdrawal_reversal`)** بدل ترك السجل الثابت (immutable) بلا مسار مصمم لحالات الاسترجاع النادرة لكن الحقيقية.
5. **عرض تفاصيل العمولة للسائق** في كل دفعة (المبلغ، نسبة العمولة، الصافي) — الشفافية تقلّل النزاعات وتُبني الثقة.
6. **تجهيز إثبات دفع قابل للتحقق بنكيًا** (معرّف معاملة من بوابة الدفع الحقيقية) قبل استبدال بوابة الدفع المحاكاة (simulation) الحالية للولي.

### منظومة الذكاء الاصطناعي
- توثيق كامل (انظر القسم 4).
- تفعيل الفعلي لتأثير نموذج XGBoost في القرار، أو توضيح صراحة في التوثيق/الواجهة أن القرار قائم على قواعد (rule-based) وليس ML بالكامل — تجنّبًا لمشاكل مصداقية قانونية لاحقًا.
- إضافة مسار استئناف (appeal) رسمي للسائق ضد قرار الذكاء الاصطناعي، منفصل عن `ai-reset` (اللي حاليًا صلاحية أدمن فقط بلا طلب رسمي من السائق نفسه).
- حماية خدمة بايثون (`ai-service/`) بمصادقة داخلية.

### عامة
- إضافة README لمجلد `ai-service/` يشرح النشر، متطلبات النماذج، وكيفية التحديث.
- توحيد استخدام `FormRequest` في كل المسارات الحساسة (بدل `request('key', default)` المباشر).

---

*تم إعداد هذا التقرير بمراجعة فعلية للكود (مش استنتاج من التوثيق فقط) — كل نقطة مرفوقة بمرجع ملف/سطر قابل للتحقق منه في الكود.*
